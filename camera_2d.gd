extends Camera2D

	#func _process(delta):
#	var velocidad= 20
	
	#if Input.is_action_pressed("Left"):
	#	position.x -=velocidad* delta
	#if Input.is_action_pressed("right"):
	#	position.x +=velocidad* delta
